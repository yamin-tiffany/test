name: "TEST"

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    environment:
      name: qa
    steps:
    - name: Prepare
      run: |
        echo "12345"
        curl http://ipinfo.io

  build:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - name: Prepare
      run: |
        echo "abcdefg"
        curl http://ipinfo.io
        
